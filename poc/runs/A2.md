# Benchmark Run — A2 (cross_repo_blast_radius)

_Auto-generated by `poc/run.sh` (tool **names** + metrics only — no code/inputs). Raw logs are gitignored under `poc/results/`. Narrative added after review._

## Task
- **Scenario:** cross_repo_blast_radius
- **Repos:** cashbot-go, ai-server · **run_dir:** `groundx-rnd/cashbot-go`
- **Prompt:** The ai-server X-Ray layout completion webhook is adding a NEW field to the JSON payload it POSTs back: `engineVersion` (a string identifying the layout model version). Find the service that consumes this layout webhook and update its payload type so the new `engineVersion` field is decoded and available. Make the minimal change required.

## Isolation (identical on every arm — design §4.5, verified by `poc/dryrun-isolation.sh`)
- `--strict-mcp-config` (only the arm's own MCP) · `--setting-sources project,local` (no user-source claude-mem hook / plugins) · fresh `claude -p` per cell (no `--continue`/`--resume`).
- Claude **built-in** tools (grep/read/edit/bash) enabled on **all** arms. `--add-dir`: cashbot-go, ai-server.

| Arm | MCP servers allowed | Forbidden |
|---|---|---|
| baseline | none | all MCP |
| cgc | codegraphcontext | serena, all others |
| serena | serena | codegraphcontext, all others |
| both | codegraphcontext + serena | all others (claude-mem, caveman, chrome) |

## Metrics

| Arm | is_error | oracle_pass | completeness | tools | mcp_tools | turns | in_tok | out_tok | cost_usd |
|---|---|---|---|---|---|---|---|---|---|
| baseline | false | 1 | 1 | 40 | 0 | 3 | 6 | 2299 | 0.7174583500000001 |
| cgc | false | 1 | 1 | 19 | 0 | 4 | 137 | 2094 | 0.59367275 |
| serena | false | 1 | 1 | 27 | 0 | 4 | 137 | 1321 | 0.37135094999999996 |
| both | false | 1 | 1 | 7 | 0 | 8 | 4341 | 1766 | 0.285396 |

## Tool-call trace per arm (names × count, in call order they are grouped)

### baseline
- `Bash` ×19
- `Read` ×15
- `ScheduleWakeup` ×3
- `Agent` ×2
- `Edit` ×1

### cgc
- `Bash` ×9
- `Read` ×6
- `Agent` ×1
- `Edit` ×1
- `ScheduleWakeup` ×1
- `ToolSearch` ×1

### serena
- `Bash` ×12
- `Read` ×11
- `ScheduleWakeup` ×2
- `Agent` ×1
- `Edit` ×1

### both
- `Bash` ×5
- `Edit` ×1
- `Read` ×1

## Narrative / takeaway

**The decisive cross-repo task — and the graph was still never used.** (Pass 1 of a planned 3; transcripts archived as `A2_<arm>.p1.jsonl`. An earlier ×3 attempt was discarded: it hit the Claude session limit mid-run and its per-pass logs were overwritten — see [`FINDINGS.md`](../FINDINGS.md).)

**Result (pass 1):** all 4 arms passed (`oracle_pass=1`, `engineVersion` field added, `go build ./pkg/...` green), all `pristine ✓`, and **all `mcp=0`** — including cgc and both.

| arm | oracle | mcp offered → called | tools | turns | cost |
|---|:---:|---|---:|---:|---:|
| baseline | ✅ | 0 → 0 | 40 | 3 | $0.717 |
| cgc | ✅ | **25 → 0** | 19 | 4 | $0.594 |
| serena | ✅ | (serena set) → 0 | 27 | 4 | $0.371 |
| both | ✅ | (cgc+serena) → 0 | 7 | 8 | $0.285 |

**Proof the graph was available and ignored:** cgc's `system/init` listed **25 `mcp__codegraphcontext__*` tools** (the manifest), yet cgc made **0** calls to any of them, and its reasoning text never once mentioned the graph. The tools were on the menu (callability independently proven by `probe-mcp.sh` the same day) — the model simply did not reach for them.

**How every arm actually solved it:** grep/find across **both** repos (both `cashbot-go` and `ai-server` are mounted via `--add-dir`). Typical path: `grep DocumentLayoutWebhook` → `pkg/processor/documentlayout/handler.go` → trace to `pkg/model/services/response.go:Response` → read the ai-server Python webhook (`document/…`) to confirm the field → add `EngineVersion *string json:"engineVersion,omitempty"`. The `both` arm did this in just 7 tool calls; baseline wandered most (40).

### ⚠ Design flaw this exposes (the headline finding)
A2 was meant to be the task where the graph wins, because `engineVersion` is **not greppable in cashbot-go** and the consumer lives across a repo boundary. But **passing both repos via `--add-dir` lets plain `grep` span the boundary** — the model greps `ai-server` directly, finds the webhook, and traces the consumer without any graph. So the cross-repo *locate cost* the graph is designed to cut is **largely absent at 2-repo scale**.

The graph's real advantage only appears at **enterprise scale** (≈100 repos, billions of lines) where you *cannot* `--add-dir` everything and grep it all — there, structural lookup beats brute-force search. This 2-repo harness does not reproduce that condition, so it under-tests the very hypothesis. **As wired, the benchmark cannot demonstrate a graph benefit, and it shows the model won't reach for the graph when local grep suffices.**

### Honest conclusion (pending passes 2–3)
At equal correctness, no arm used the graph and the cost differences track exploration depth/turns (same noise as R1–R3), not graph usage. To actually test the hypothesis we would need a setup that denies brute-force grep across all repos (e.g. mount only the run repo and force cross-repo discovery through the graph, or scale to many repos). See [`FINDINGS.md`](../FINDINGS.md) for the cross-task conclusion and proposed redesign.
