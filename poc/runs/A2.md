# Benchmark Run — A2 (cross_repo_blast_radius)

_Auto-generated by `poc/run.sh` (tool **names** + metrics only — no code/inputs). Raw logs are gitignored under `poc/results/`. Narrative added after review._

## Task
- **Scenario:** cross_repo_blast_radius
- **Repos:** cashbot-go, ai-server · **run_dir:** `groundx-rnd/cashbot-go`
- **Prompt:** The ai-server X-Ray layout completion webhook is adding a NEW field to the JSON payload it POSTs back: `engineVersion` (a string identifying the layout model version). Find the service that consumes this layout webhook and update its payload type so the new `engineVersion` field is decoded and available. Make the minimal change required.

## Isolation (identical on every arm — design §4.5, verified by `poc/dryrun-isolation.sh`)
- `--strict-mcp-config` (only the arm's own MCP) · `--setting-sources project,local` (no user-source claude-mem hook / plugins) · fresh `claude -p` per cell (no `--continue`/`--resume`).
- Claude **built-in** tools (grep/read/edit/bash) enabled on **all** arms. `--add-dir`: cashbot-go, ai-server.

| Arm | MCP servers allowed | Forbidden |
|---|---|---|
| baseline | none | all MCP |
| cgc | codegraphcontext | serena, all others |
| serena | serena | codegraphcontext, all others |
| both | codegraphcontext + serena | all others (claude-mem, caveman, chrome) |

## Metrics

| Arm | is_error | oracle_pass | completeness | tools | mcp_tools | turns | in_tok | out_tok | cost_usd |
|---|---|---|---|---|---|---|---|---|---|
| baseline | false | 1 | 1 | 33 | 0 | 3 | 135 | 1459 | 0.7389381499999998 |
| cgc | false | 1 | 1 | 8 | 0 | 9 | 3962 | 2197 | 0.4053785 |
| serena | false | 0 | 0 | 9 | 0 | 10 | 3255 | 2355 | 0.42428749999999993 |
| both | false | 0 | 0 | 17 | 0 | 4 | 137 | 1531 | 0.34614409999999995 |

## Tool-call trace per arm (names × count, in call order they are grouped)

### baseline
- `Bash` ×17
- `Read` ×13
- `Agent` ×2
- `Edit` ×1

### cgc
- `Bash` ×5
- `Read` ×2
- `Edit` ×1

### serena
- `Bash` ×6
- `Read` ×2
- `Edit` ×1

### both
- `Bash` ×8
- `Read` ×6
- `Agent` ×1
- `Edit` ×1
- `Monitor` ×1

## Narrative / takeaway

**Fresh validated run (2026-06-29 eve, quality oracle = `*string` required): baseline ✅, cgc ✅, serena ❌, both ❌; all `mcp=0`.** The cross-repo consumer is greppable in cashbot-go (both repos mounted), so no arm needed the graph — same as before. The new result is that the **tightened oracle now discriminates quality**: serena and both added the `engineVersion` field but **not as `*string` + omitempty**, so they correctly fail where the old field-tag-only oracle passed them. A2 remains a non-discriminator for the *graph* (grep spans the mounted repos) — that's why A3 exists. See [`FINDINGS.md`](../FINDINGS.md).
