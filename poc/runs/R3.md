# Benchmark Run — R3 (bugfix)

_Auto-generated by `poc/run.sh` (tool **names** + metrics only — no code/inputs). Raw logs are gitignored under `poc/results/`. Narrative added after review._

## Task
- **Scenario:** bugfix
- **Repos:** cashbot-go · **run_dir:** `groundx-rnd/cashbot-go`
- **Prompt:** Valid OAuth token requests are sometimes rejected with 'invalid_grant' because the request's resource URL does not match the stored one when they differ only by normalization. Fix the OAuth authorization_code and refresh_token grant handling so MCP resource URLs are normalized before comparison. Do not modify any _test.go file.

## Isolation (identical on every arm — design §4.5, verified by `poc/dryrun-isolation.sh`)
- `--strict-mcp-config` (only the arm's own MCP) · `--setting-sources project,local` (no user-source claude-mem hook / plugins) · fresh `claude -p` per cell (no `--continue`/`--resume`).
- Claude **built-in** tools (grep/read/edit/bash) enabled on **all** arms. `--add-dir`: cashbot-go.

| Arm | MCP servers allowed | Forbidden |
|---|---|---|
| baseline | none | all MCP |
| cgc | codegraphcontext | serena, all others |
| serena | serena | codegraphcontext, all others |
| both | codegraphcontext + serena | all others (claude-mem, caveman, chrome) |

## Metrics

| Arm | is_error | oracle_pass | completeness | tools | mcp_tools | turns | in_tok | out_tok | cost_usd |
|---|---|---|---|---|---|---|---|---|---|
| baseline | false | 1 | 1 | 10 | 0 | 11 | 2632 | 7656 | 0.8937350000000001 |
| cgc | false | 1 | 1 | 11 | 0 | 12 | 4101 | 7681 | 0.9740959999999999 |
| serena | false | 1 | 1 | 13 | 0 | 14 | 3265 | 11743 | 1.127982 |
| both | false | 1 | 1 | 11 | 0 | 12 | 4478 | 6217 | 0.889709 |

## Tool-call trace per arm (names × count, in call order they are grouped)

### baseline
- `Bash` ×8
- `Read` ×2

### cgc
- `Bash` ×9
- `Read` ×2

### serena
- `Bash` ×6
- `Edit` ×6
- `Read` ×1

### both
- `Bash` ×9
- `Read` ×2

## Narrative / takeaway

**2 runs, 8 cells, all `mcp=0`, all `pristine ✓`.** The hardest in-repo bug (OAuth resource-URL normalization, `oauth.go`). Graph/LSP never called (genuine choice — tools proven callable by `probe-mcp.sh`).

**Correctness is run-variable here — the failures were noise:**

| run | baseline | cgc | serena | both | pass |
|---|:---:|:---:|:---:|:---:|:---:|
| run1 (06:07Z) | ✅ | ❌ | ❌ | ✅ | 2/4 |
| run2 (06:57Z) | ✅ | ✅ | ✅ | ✅ | 4/4 |

Per-arm pass rate over 2 runs: baseline 2/2, both 2/2, cgc 1/2, serena 1/2. The run-1 failures of cgc and serena **did not reproduce** in run-2 — they were not arm-driven (a graph arm, `both`, passed both times; `mcp=0` so no arm had graph help/hindrance). R3 is simply hard enough that some runs take a wrong path regardless of arm.

**Lesson (carried into A2):** on hard tasks even `oracle_pass` is noisy — a single run's pass/fail can be misleading (run-1 alone would have wrongly suggested "cgc/serena fail R3"). Hard tasks need a multi-run pass *rate*. Also note failing cells cost more (wrong path = more turns), so never compare cost across a mix of pass/fail cells. Full numbers: `poc/results/results.csv` (rows tagged `R3`). See [`FINDINGS.md`](../FINDINGS.md).
