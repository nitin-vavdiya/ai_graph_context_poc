# Benchmark Run — R3 (bugfix)

_Auto-generated by `poc/run.sh` (tool **names** + metrics only — no code/inputs). Raw logs are gitignored under `poc/results/`. Narrative added after review._

## Task
- **Scenario:** bugfix
- **Repos:** cashbot-go · **run_dir:** `groundx-rnd/cashbot-go`
- **Prompt:** Valid OAuth token requests are sometimes rejected with 'invalid_grant' because the request's resource URL does not match the stored one when they differ only by normalization. Fix the OAuth authorization_code and refresh_token grant handling so MCP resource URLs are normalized before comparison. Do not modify any _test.go file.

## Isolation (identical on every arm — design §4.5, verified by `poc/dryrun-isolation.sh`)
- `--strict-mcp-config` (only the arm's own MCP) · `--setting-sources project,local` (no user-source claude-mem hook / plugins) · fresh `claude -p` per cell (no `--continue`/`--resume`).
- Claude **built-in** tools (grep/read/edit/bash) enabled on **all** arms. `--add-dir`: cashbot-go.

| Arm | MCP servers allowed | Forbidden |
|---|---|---|
| baseline | none | all MCP |
| cgc | codegraphcontext | serena, all others |
| serena | serena | codegraphcontext, all others |
| both | codegraphcontext + serena | all others (claude-mem, caveman, chrome) |

## Metrics

| Arm | is_error | oracle_pass | completeness | tools | mcp_tools | turns | in_tok | out_tok | cost_usd |
|---|---|---|---|---|---|---|---|---|---|
| baseline | false | 1 | 1 | 9 | 0 | 10 | 2628 | 5740 | 1.212919 |
| cgc | false | 0 | 0 | 15 | 0 | 16 | 4238 | 11161 | 1.329903 |
| serena | false | 0 | 0 | 18 | 0 | 19 | 3404 | 12634 | 1.3419485000000002 |
| both | false | 1 | 1 | 10 | 0 | 11 | 4476 | 5192 | 0.79449 |

## Tool-call trace per arm (names × count, in call order they are grouped)

### baseline
- `Bash` ×6
- `Read` ×3

### cgc
- `Bash` ×9
- `Edit` ×5
- `Read` ×1

### serena
- `Bash` ×8
- `Edit` ×8
- `Read` ×2

### both
- `Bash` ×9
- `Read` ×1

## Narrative / takeaway

**First task with failures: 2 of 4 arms did not fix it** (oracle_pass=0 for cgc and serena; baseline and both passed). All 8 pre-cell `assert_pristine` checks `✓`; all arms `mcp_tools=0` (now **five single-repo task-runs (R1×2, R2×2, R3×1) = 20 cells, zero graph calls**).

#### Token/cost breakdown (`total billable` = in + out + cache_read)

| arm | oracle | in | out | cache_read | total billable | tools/turns | cost |
|---|:---:|---:|---:|---:|---:|---:|---:|
| baseline | ✅ | 2,628 | 5,740 | 521,686 | 530,054 | 9/10 | $1.213 |
| cgc | ❌ | 4,238 | 11,161 | 922,404 | 937,803 | 15/16 | $1.330 |
| serena | ❌ | 3,404 | 12,634 | 1,124,305 | 1,140,343 | 18/19 | $1.342 |
| both | ✅ | 4,476 | 5,192 | 575,268 | 585,036 | 10/11 | $0.794 |

#### Reading it

- **The failures are NOT arm-driven.** The graph arms are cgc, serena, **and both** — yet `both` *passed* while the non-graph `baseline` also passed; cgc and serena failed. There is no "graph arms fail" pattern (a graph arm passed, a non-graph arm passed). With `mcp=0` everywhere, no arm had any graph help or hindrance to begin with. **Pass/fail here is task difficulty + LLM non-determinism, not the arm.** R3 (OAuth resource-URL normalization) is subtle enough that some runs take a wrong path.
- **Failing runs cost MORE.** cgc (16 turns / 11.2K out) and serena (19 turns / 12.6K out) both wandered far more than the passing baseline (10 / 5.7K) and both (11 / 5.2K). A wrong path burns more turns *and* still fails. **Corollary: never compare `cost` across a mix of pass and fail cells** — the cheap cells here are the correct ones; cost and correctness are entangled.
- **`in_tokens` stable as ever** (baseline 2.6K < serena 3.4K < cgc/both 4.2–4.5K) — the only reliably arm-specific number.

#### Methodology consequence (important)
R3 shows **`oracle_pass` is itself noisy on hard tasks** — a single run gave a 50% pass rate that maps to no arm property. A one-shot result here would be actively misleading (e.g. "cgc/serena fail R3" is false — they fail *this run*). **Hard tasks need a multi-run pass *rate* per arm**, not a single pass/fail. This strengthens the case for running the decisive task A2 ≥3× (and arguably R3 too) before drawing any conclusion.

**Conclusion:** R3 reinforces the in-repo null on the graph dimension (`mcp=0`, no graph use) and adds a sharper lesson: on harder tasks even correctness is run-variable, so single-run rankings — of cost *or* pass/fail — are unsafe. Verdict still rides on a multi-run A2.
