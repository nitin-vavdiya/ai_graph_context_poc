# Benchmark Run — A3 (cross_repo_blast_radius)

_Auto-generated by `poc/run.sh` (tool **names** + metrics only — no code/inputs). Raw logs are gitignored under `poc/results/`. Narrative added after review._

## Task
- **Scenario:** cross_repo_blast_radius
- **Repos:** cashbot-go · **run_dir:** `groundx-rnd/cashbot-go`
- **Prompt:** ai-server's document layout pipeline now includes an ADDITIONAL field in the JSON result it POSTs back to cashbot-go's DocumentLayoutWebhook callback. The field was added to ai-server's DocumentResponse model. Update cashbot-go's webhook response type at pkg/model/services/response.go so the new field is decoded and available. Determine the field's exact name and type from ai-server's DocumentResponse model and mirror it with the correct JSON tag (match the existing style: pointer + omitempty for optional fields). Make the minimal change. Do not modify any _test.go file.

## Isolation (identical on every arm — design §4.5, verified by `poc/dryrun-isolation.sh`)
- `--strict-mcp-config` (only the arm's own MCP) · `--setting-sources project,local` (no user-source claude-mem hook / plugins) · fresh `claude -p` per cell (no `--continue`/`--resume`).
- Claude **built-in** tools (grep/read/edit/bash) enabled on **all** arms. `--add-dir`: cashbot-go.

| Arm | MCP servers allowed | Forbidden |
|---|---|---|
| baseline | none | all MCP |
| cgc | codegraphcontext | serena, all others |
| serena | serena | codegraphcontext, all others |
| both | codegraphcontext + serena | all others (claude-mem, caveman, chrome) |

## Metrics

| Arm | is_error | oracle_pass | completeness | tools | mcp_tools | turns | in_tok | out_tok | cost_usd |
|---|---|---|---|---|---|---|---|---|---|
| baseline | false | 0 | 0 | 56 | 0 | 15 | 288 | 9848 | 1.5012678999999995 |
| cgc | false | 1 | 1 | 23 | 3 | 17 | 5898 | 6520 | 0.86794655 |
| serena | false | 1 | 1 | 22 | 0 | 9 | 4372 | 2708 | 0.3759755 |
| both | false | 0 | 0 | 11 | 0 | 12 | 4476 | 5310 | 0.5075974999999999 |

## Tool-call trace per arm (names × count, in call order they are grouped)

### baseline
- `Bash` ×45
- `Read` ×8
- `Agent` ×1
- `ScheduleWakeup` ×1
- `ToolSearch` ×1

### cgc
- `Bash` ×12
- `Read` ×4
- `ToolSearch` ×2
- `mcp__codegraphcontext__execute_cypher_query` ×2
- `Agent` ×1
- `Edit` ×1
- `mcp__codegraphcontext__list_indexed_repositories` ×1

### serena
- `Bash` ×15
- `Read` ×5
- `Agent` ×1
- `Edit` ×1

### both
- `Bash` ×10
- `Read` ×1

## Narrative / takeaway

**Fresh validated run (2026-06-29 eve): cgc ✅ via the graph (`mcp=3`) — the one clean positive of the whole POC. baseline ❌, serena ✅ (leak), both ❌.**

- **cgc (legit):** with ai-server off-disk, cgc queried the graph (`mcp__codegraphcontext__*` ×3) to retrieve `taskDuration` from the indexed-but-absent ai-server `DocumentResponse`, and added the field. This is the hypothesis demonstrated: the graph let the agent solve a task where the source is not on disk.
- **baseline (❌):** no graph, ai-server off-disk → couldn't determine the field, failed.
- **serena (✅ but INVALID — leak):** passed with `mcp=0` by `find`-ing the *parked* copy at `poc/results/.parked/ai-server/...` (still readable) and a stray checkout under `log_analysis/` — not via its LSP. Confirms `park_repo` does not isolate (the agent searches the whole filesystem).
- **both (❌):** had the graph but didn't reach for it.

**Caveat:** isolation is still leaky (file-moving is insufficient; needs container/permission sandboxing), so only cgc's pass is trustworthy. Even so, A3 is the clearest evidence that **the graph retrieves off-disk code that grep cannot**. See [`FINDINGS.md`](../FINDINGS.md).
