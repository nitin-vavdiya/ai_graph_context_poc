# Benchmark Run — R2 (bugfix)

_Auto-generated by `poc/run.sh` (tool **names** + metrics only — no code/inputs). Raw logs are gitignored under `poc/results/`. Narrative added after review._

## Task
- **Scenario:** bugfix
- **Repos:** cashbot-go · **run_dir:** `groundx-rnd/cashbot-go`
- **Prompt:** For partner MCP accounts, an auto-generated OpenAPI tool is being exposed that duplicates (shadows) a partner-specific tool; partner accounts should not see that shadowed customer operation. Update the per-account MCP tool registration so the shadowed operation is hidden for partner accounts. Do not modify any _test.go file.

## Isolation (identical on every arm — design §4.5, verified by `poc/dryrun-isolation.sh`)
- `--strict-mcp-config` (only the arm's own MCP) · `--setting-sources project,local` (no user-source claude-mem hook / plugins) · fresh `claude -p` per cell (no `--continue`/`--resume`).
- Claude **built-in** tools (grep/read/edit/bash) enabled on **all** arms. `--add-dir`: cashbot-go.

| Arm | MCP servers allowed | Forbidden |
|---|---|---|
| baseline | none | all MCP |
| cgc | codegraphcontext | serena, all others |
| serena | serena | codegraphcontext, all others |
| both | codegraphcontext + serena | all others (claude-mem, caveman, chrome) |

## Metrics

| Arm | is_error | oracle_pass | completeness | tools | mcp_tools | turns | in_tok | out_tok | cost_usd |
|---|---|---|---|---|---|---|---|---|---|
| baseline | false | 1 | 1 | 44 | 0 | 13 | 278 | 11788 | 1.8632202000000002 |
| cgc | false | 1 | 1 | 42 | 0 | 19 | 7548 | 10709 | 1.5488400500000004 |
| serena | false | 1 | 1 | 41 | 0 | 17 | 7066 | 20737 | 1.8042157000000003 |
| both | false | 1 | 1 | 40 | 0 | 12 | 278 | 18228 | 1.8649634499999999 |

## Tool-call trace per arm (names × count, in call order they are grouped)

### baseline
- `Read` ×21
- `Bash` ×19
- `Edit` ×2
- `Agent` ×1
- `ToolSearch` ×1

### cgc
- `Read` ×23
- `Bash` ×16
- `Edit` ×2
- `Agent` ×1

### serena
- `Read` ×24
- `Bash` ×14
- `Edit` ×2
- `Agent` ×1

### both
- `Bash` ×19
- `Read` ×17
- `Edit` ×2
- `Agent` ×1
- `ToolSearch` ×1

## Narrative / takeaway


**3 runs, all 12 cells pass, all `mcp=0`, all `pristine ✓`.** Single-file in-repo bug (`pkg/mcp/mcp.go`); graph/LSP never called on any arm, in any run — confirmed a genuine choice (tools proven callable by `probe-mcp.sh`).

**Cost ranking flips violently between runs → noise, not arm effect.** `both` was the *cheapest* arm in run2 ($0.627, 12 turns) and the *most expensive* in run3 ($1.385, 19 turns / 14.7K out tokens). cgc was priciest in runs 1–2 then mid in run3. No arm holds a rank. The driver is turn count → `cache_read` → cost, and turns are random per run. `in_tokens` is the only stable signal (baseline ~2.6K < serena ~3.4K < cgc/both ~4.2–4.6K). Full per-run numbers in `poc/results/results.csv` (rows tagged `R2`); Metrics table above is the latest run.

**Takeaway:** same as R1 — no graph use, no benefit, ranks are noise. The earlier "cgc consistently priciest" hint did **not** survive the third run, reinforcing that single/double runs can't rank arms on easy tasks. See [`FINDINGS.md`](../FINDINGS.md).
