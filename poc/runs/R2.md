# Benchmark Run — R2 (bugfix)

_Auto-generated by `poc/run.sh` (tool **names** + metrics only — no code/inputs). Raw logs are gitignored under `poc/results/`. Narrative added after review._

## Task
- **Scenario:** bugfix
- **Repos:** cashbot-go · **run_dir:** `groundx-rnd/cashbot-go`
- **Prompt:** For partner MCP accounts, an auto-generated OpenAPI tool is being exposed that duplicates (shadows) a partner-specific tool; partner accounts should not see that shadowed customer operation. Update the per-account MCP tool registration so the shadowed operation is hidden for partner accounts. Do not modify any _test.go file.

## Isolation (identical on every arm — design §4.5, verified by `poc/dryrun-isolation.sh`)
- `--strict-mcp-config` (only the arm's own MCP) · `--setting-sources project,local` (no user-source claude-mem hook / plugins) · fresh `claude -p` per cell (no `--continue`/`--resume`).
- Claude **built-in** tools (grep/read/edit/bash) enabled on **all** arms. `--add-dir`: cashbot-go.

| Arm | MCP servers allowed | Forbidden |
|---|---|---|
| baseline | none | all MCP |
| cgc | codegraphcontext | serena, all others |
| serena | serena | codegraphcontext, all others |
| both | codegraphcontext + serena | all others (claude-mem, caveman, chrome) |

## Metrics

| Arm | is_error | oracle_pass | completeness | tools | mcp_tools | turns | in_tok | out_tok | cost_usd |
|---|---|---|---|---|---|---|---|---|---|
| baseline | false | 1 | 1 | 12 | 0 | 13 | 2636 | 5228 | 1.112637 |
| cgc | false | 1 | 1 | 18 | 0 | 19 | 4244 | 7543 | 1.1976629999999997 |
| serena | false | 1 | 1 | 8 | 0 | 9 | 3255 | 4733 | 0.6671355 |
| both | false | 1 | 1 | 13 | 0 | 14 | 4482 | 6750 | 0.8816275000000001 |

## Tool-call trace per arm (names × count, in call order they are grouped)

### baseline
- `Read` ×6
- `Bash` ×5
- `Edit` ×1

### cgc
- `Bash` ×10
- `Read` ×7
- `Edit` ×1

### serena
- `Bash` ×4
- `Read` ×3
- `Edit` ×1

### both
- `Bash` ×7
- `Read` ×5
- `Edit` ×1

## Narrative / takeaway

**All 4 arms passed** (oracle_pass=1, completeness=1); all 8 pre-cell `assert_pristine` checks were `✓` (no cross-arm contamination). Like R1, this is a single-repo fix in one file (`pkg/mcp/mcp.go`) and **every arm used `mcp_tools=0`** — the graph/LSP servers were loaded but Claude located the code with built-in `grep`/`read`/`edit` and never queried them. Consistent with R1 across now three single-repo runs: on in-repo work Claude does not reach for the graph.

#### Token/cost breakdown (`total billable` = in + out + cache_read)

| arm | in | out | cache_read | total billable | tools/turns | cost |
|---|---:|---:|---:|---:|---:|---:|
| baseline | 2,636 | 5,228 | 615,546 | 623,410 | 12/13 | $1.113 |
| cgc | 4,244 | 7,543 | 955,828 | 967,615 | 18/19 | $1.198 |
| serena | 3,255 | 4,733 | 343,713 | 351,701 | 8/9 | $0.667 |
| both | 4,482 | 6,750 | 667,127 | 678,359 | 13/14 | $0.882 |

**Cost spread is wider than R1** (serena $0.667 → cgc $1.198, ~1.8×), but it tracks **turns/exploration depth, not graph usage** (mcp=0 everywhere). The ranking is driven by how much each run happened to wander: cgc took the most turns (19) → highest cache_read (955K) → highest cost; serena finished fastest (9 turns) → cheapest. This is the same run-to-run noise seen in R1, just with a bigger amplitude on a slightly harder task — `in_tokens` stays stable per arm (the fixed MCP-schema tax: baseline 2.6K < serena 3.3K < cgc/both 4.2–4.5K), while turns/out/cache_read swing.

**Mild signal worth noting (one run, not conclusive):** the cgc arm did *more* exploration (18 tools / 19 turns) than baseline (12/13) yet still never used the graph — so on this in-repo task the graph MCP was pure overhead (schema tax + a longer wander), not a help. Whether that's the arm or just variance needs multi-run averaging; the decisive test remains **A2** (cross-repo), where the graph can actually change the search path.
