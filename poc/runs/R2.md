# Benchmark Run — R2 (bugfix)

_Auto-generated by `poc/run.sh` (tool **names** + metrics only — no code/inputs). Raw logs are gitignored under `poc/results/`. Narrative added after review._

## Task
- **Scenario:** bugfix
- **Repos:** cashbot-go · **run_dir:** `groundx-rnd/cashbot-go`
- **Prompt:** For partner MCP accounts, an auto-generated OpenAPI tool is being exposed that duplicates (shadows) a partner-specific tool; partner accounts should not see that shadowed customer operation. Update the per-account MCP tool registration so the shadowed operation is hidden for partner accounts. Do not modify any _test.go file.

## Isolation (identical on every arm — design §4.5, verified by `poc/dryrun-isolation.sh`)
- `--strict-mcp-config` (only the arm's own MCP) · `--setting-sources project,local` (no user-source claude-mem hook / plugins) · fresh `claude -p` per cell (no `--continue`/`--resume`).
- Claude **built-in** tools (grep/read/edit/bash) enabled on **all** arms. `--add-dir`: cashbot-go.

| Arm | MCP servers allowed | Forbidden |
|---|---|---|
| baseline | none | all MCP |
| cgc | codegraphcontext | serena, all others |
| serena | serena | codegraphcontext, all others |
| both | codegraphcontext + serena | all others (claude-mem, caveman, chrome) |

## Metrics

| Arm | is_error | oracle_pass | completeness | tools | mcp_tools | turns | in_tok | out_tok | cost_usd |
|---|---|---|---|---|---|---|---|---|---|
| baseline | false | 1 | 1 | 13 | 0 | 14 | 2638 | 7401 | 1.0022484999999999 |
| cgc | false | 1 | 1 | 18 | 0 | 19 | 4244 | 10977 | 1.2323715 |
| serena | false | 1 | 1 | 19 | 0 | 20 | 3406 | 7372 | 0.9273299999999999 |
| both | false | 1 | 1 | 11 | 0 | 12 | 4478 | 4880 | 0.6273769999999999 |

## Tool-call trace per arm (names × count, in call order they are grouped)

### baseline
- `Bash` ×6
- `Read` ×6
- `Edit` ×1

### cgc
- `Read` ×9
- `Bash` ×6
- `Edit` ×3

### serena
- `Bash` ×12
- `Read` ×5
- `Edit` ×2

### both
- `Bash` ×7
- `Read` ×3
- `Edit` ×1

## Narrative / takeaway

**All 4 arms passed both runs** (oracle_pass=1, completeness=1); all 16 pre-cell `assert_pristine` checks `✓`. Single-repo fix in one file (`pkg/mcp/mcp.go`); **every arm, both runs, used `mcp_tools=0`** — graph/LSP loaded but never queried. Across R1 (×2) and R2 (×2) that is now **four single-repo runs, zero graph calls**.

#### Two runs — token/cost (`total billable` = in + out + cache_read)

**Run 1** (05:39–05:44Z)

| arm | in | out | cache_read | total billable | turns | cost |
|---|---:|---:|---:|---:|---:|---:|
| baseline | 2,636 | 5,228 | 615,546 | 623,410 | 13 | $1.113 |
| cgc | 4,244 | 7,543 | 955,828 | 967,615 | 19 | $1.198 |
| serena | 3,255 | 4,733 | 343,713 | 351,701 | 9 | $0.667 |
| both | 4,482 | 6,750 | 667,127 | 678,359 | 14 | $0.882 |

**Run 2** (05:51–05:58Z)

| arm | in | out | cache_read | total billable | turns | cost |
|---|---:|---:|---:|---:|---:|---:|
| baseline | 2,638 | 7,401 | 718,019 | 728,058 | 14 | $1.002 |
| cgc | 4,244 | 10,977 | 999,645 | 1,014,866 | 19 | $1.232 |
| serena | 3,406 | 7,372 | 892,052 | 902,830 | 20 | $0.927 |
| both | 4,478 | 4,880 | 478,546 | 487,904 | 12 | $0.627 |

#### Reading it

- **Cost ranking flips between runs → confirms noise.** Cheap→expensive was `serena < both < baseline < cgc` (run 1) but `both < serena < baseline < cgc` (run 2). serena went from cheapest (9 turns) to 2nd (20 turns); both went from 3rd to cheapest. Same arm, same task — rank decided by how many turns that run happened to take. **No arm-attributable cost reduction exists here.**
- **One consistent signal: cgc is the most expensive in *both* runs** (and steady at 19 turns each). On this in-repo task the cgc arm repeatably wanders the most and costs the most while never using the graph — i.e. the graph MCP is a *net tax* here (biggest `in_tokens` schema floor 4.2K + longest exploration), not a help. Mild (one task), but unlike the serena/both swings it did not flip.
- **`in_tokens` stays stable per arm** across both runs (baseline 2.6K < serena 3.3–3.4K < cgc/both 4.2–4.5K) — the only reliably arm-specific number; everything else (out/turns/cache_read/cost) is turn-driven noise.

**Conclusion:** R2 (×2) reinforces R1 — the harness and isolation are sound, but there is **no evidence the graph reduces cost on single-repo tasks**, and a mild repeatable signal that cgc *adds* cost there. Any genuine graph benefit must show up on **A2** (cross-repo) as `mcp>0` + fewer turns/cost at equal `oracle_pass`, and must exceed this run-to-run noise (→ run A2 ≥3× and take the median).
