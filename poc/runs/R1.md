# Benchmark Run — R1 (bugfix)

_Auto-generated by `poc/run.sh` (tool **names** + metrics only — no code/inputs). Raw logs are gitignored under `poc/results/`. Narrative added after review._

## Task
- **Scenario:** bugfix
- **Repos:** cashbot-go · **run_dir:** `groundx-rnd/cashbot-go`
- **Prompt:** In the GroundX partner integration, executing an MCP tool sometimes fails to decode the upstream response (error: "failed to parse MCP execution response"). Diagnose the cause and fix the response handling so MCP tool executions decode correctly. Do not modify any _test.go file.

## Isolation (identical on every arm — design §4.5, verified by `poc/dryrun-isolation.sh`)
- `--strict-mcp-config` (only the arm's own MCP) · `--setting-sources project,local` (no user-source claude-mem hook / plugins) · fresh `claude -p` per cell (no `--continue`/`--resume`).
- Claude **built-in** tools (grep/read/edit/bash) enabled on **all** arms. `--add-dir`: cashbot-go.

| Arm | MCP servers allowed | Forbidden |
|---|---|---|
| baseline | none | all MCP |
| cgc | codegraphcontext | serena, all others |
| serena | serena | codegraphcontext, all others |
| both | codegraphcontext + serena | all others (claude-mem, caveman, chrome) |

## Metrics

| Arm | is_error | oracle_pass | completeness | tools | mcp_tools | turns | in_tok | out_tok | cost_usd |
|---|---|---|---|---|---|---|---|---|---|
| baseline | false | 1 | 1 | 12 | 0 | 13 | 2636 | 7235 | 0.7143124999999999 |
| cgc | false | 1 | 1 | 15 | 0 | 16 | 4238 | 8129 | 0.7251535000000001 |
| serena | false | 1 | 1 | 19 | 0 | 20 | 3535 | 13687 | 1.142027 |
| both | false | 1 | 1 | 16 | 0 | 17 | 4617 | 9352 | 0.9909690000000001 |

## Tool-call trace per arm (names × count, in call order they are grouped)

### baseline
- `Edit` ×4
- `Bash` ×3
- `Read` ×3
- `Grep` ×2

### cgc
- `Bash` ×7
- `Edit` ×4
- `Read` ×3
- `Grep` ×1

### serena
- `Read` ×7
- `Bash` ×4
- `Edit` ×4
- `Grep` ×4

### both
- `Bash` ×4
- `Edit` ×4
- `Read` ×4
- `Grep` ×3
- `Glob` ×1

## Narrative / takeaway



**3 runs, all 12 cells pass, all `mcp=0`, all `pristine ✓`.** Single-file in-repo bug (`mcp.go`); every arm located it with grep/read/edit and never called the graph/LSP — even after `probe-mcp.sh` *proved* the tools are callable (so `mcp=0` is a genuine choice, not a broken server).

**Cost is pure noise here — the cheapest arm flips every run** (`cost_usd`): run1 serena $0.497, run2 cgc $0.466, run3 baseline $0.742. Same task, no graph use; the ranking is decided by how many turns each run randomly took (which drives `cache_read`, ~90% of cost). Only `in_tokens` is stable per arm across all 3 runs (baseline ~2.6K < serena ~3.3K < cgc ~4.1–4.2K < both ~4.5–4.6K) — the fixed MCP-schema tax. Full per-run numbers: `poc/results/results.csv` (rows tagged `R1`). The Metrics table above is the latest run only.

**Takeaway:** easy in-repo task → graph never used, arms within noise, no benefit (mild schema tax for cgc/both). Expected null. See [`FINDINGS.md`](../docs/03-task-runs.md) for the cross-task conclusion.
