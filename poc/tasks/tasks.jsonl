{"id": "C1", "scenario": "locate_and_fix", "repos": ["cashbot-go"], "run_dir": "groundx-rnd/cashbot-go", "prompt": "The tests in the Go package ./pkg/formatter are failing. Find the root cause in the non-test source and fix it so the package's tests pass. Do not modify any _test.go file.", "setup": "Seed a one-line bug: in pkg/formatter/format.go, function Defaults, change `if eu.First != \"\"` to `if eu.First == \"\"`. Confirm `go test ./pkg/formatter` is RED before the run.", "oracle": {"test_cmd": ["go test ./pkg/formatter"], "must_change": ["pkg/formatter/format.go:Defaults"], "regression_cmd": ["go test ./pkg/formatter/..."]}, "stresses": "time", "ground_truth_notes": "Restore the guard to `if eu.First != \"\"` (or logically-equivalent correct behavior). Find-cost is the signal: tokens/tool-calls to locate format.go:Defaults from a failing-test starting point. VERIFIED 2026-06-26: this exact bug turns Test_DecorateString red and the restore turns it green."}
{"id": "B1", "scenario": "in_repo_impact", "repos": ["cashbot-go"], "run_dir": "groundx-rnd/cashbot-go", "prompt": "Add a boolean parameter `secure` to the `BaseURL()` method on the Upload configuration type, changing its signature to `BaseURL(secure bool) string`: return an `https://` URL when `secure` is true and `http://` when false. Update every call site in the repository to pass `true`. Everything must still compile and the config package tests must pass.", "setup": "Clean checkout; confirm `go build ./pkg/...` and `go test ./pkg/config/...` are green before the run.", "oracle": {"test_cmd": ["go build ./pkg/...", "go test ./pkg/config/..."], "must_change": ["pkg/config/upload.go:BaseURL", "all ~20 caller files (11 under pkg/, ~9 under cmd|lambda|integration)"], "regression_cmd": ["go build ./pkg/...", "go test ./pkg/config/..."]}, "stresses": "quality", "ground_truth_notes": "Completeness is the signal: after a correct change, `grep -rn --include='*.go' '\\.BaseURL()' .` (empty parens) must return 0 — any remaining no-arg call is a missed site. Tests/find-all-references (Serena/CGC) should beat grep here. BaseURL defined at pkg/config/upload.go:34; ~49 call lines across ~20 files."}
{"id": "A1", "scenario": "cross_repo_blast_radius", "repos": ["cashbot-go", "ai-server"], "run_dir": "groundx-rnd/cashbot-go", "prompt": "The ai-server X-Ray layout service is renaming a field in the JSON webhook payload it POSTs back to this service's layout-completion webhook: the field currently sent as `resultURL` will be sent as `resultUri`. Update this (cashbot-go) service so it still correctly decodes that incoming webhook payload. Make the minimal change required.", "setup": "Clean checkout; `--add-dir` includes both groundx-rnd/cashbot-go and groundx-rnd/ai-server so any arm CAN read across repos.", "oracle": {"test_cmd": ["go build ./pkg/..."], "must_change": ["pkg/model/services/response.go:Response.ResultURL json tag"], "regression_cmd": ["go build ./pkg/..."]}, "stresses": "cost", "ground_truth_notes": "Correct edit: in pkg/model/services/response.go the `ResultURL` field's tag becomes `json:\"resultUri,omitempty\"` (was `json:\"resultURL,omitempty\"`). Completeness check: response.go contains `json:\"resultUri` and no longer `json:\"resultURL`. CAVEAT (honest): the shared field name `resultURL` is greppable, so baseline can locate it cheaply — A1 is the EASY/greppable control: it validates the cross-repo harness and measures tool-calls/tokens; we expect arms to roughly TIE. Pair with A2 (hard/relational) for the contrast."}
{"id": "A2", "scenario": "cross_repo_blast_radius", "repos": ["cashbot-go", "ai-server"], "run_dir": "groundx-rnd/cashbot-go", "prompt": "The ai-server X-Ray layout completion webhook is adding a NEW field to the JSON payload it POSTs back: `engineVersion` (a string identifying the layout model version). Find the service that consumes this layout webhook and update its payload type so the new `engineVersion` field is decoded and available. Make the minimal change required.", "setup": "Clean checkout; `--add-dir` includes both groundx-rnd/cashbot-go and groundx-rnd/ai-server. VERIFIED 2026-06-26: `engineVersion` occurs 0 times in either repo, so it cannot be grepped in the consumer.", "oracle": {"test_cmd": ["go build ./pkg/..."], "must_change": ["pkg/model/services/response.go:Response (new EngineVersion field, json:\"engineVersion\")"], "regression_cmd": ["go build ./pkg/..."]}, "stresses": "time", "ground_truth_notes": "Correct edit: add a field EngineVersion of type *string with json tag engineVersion,omitempty to the Response struct in pkg/model/services/response.go. Completeness check: response.go contains `json:\"engineVersion`. This is the HARD/relational task: the new field is absent from cashbot-go, so it is NOT greppable there — the agent must trace the relationship (ai-server layout webhook -> which service receives it -> the Response struct decoded by lambda/DocumentLayoutWebhook). CGC's CALLS_SERVICE edge (ai-server->cashbot-go) answers the 'who consumes it' question directly; baseline must infer it (e.g. ai-server's callbackURL -> api.groundx.ai = cashbot-go per the C4 model); Serena cannot span repos to confirm the sender (documented limit). NOTE: Go's json.Unmarshal silently ignores unknown fields, so the prompt explicitly asks to capture/expose the field, making the struct edit the required outcome."}
